<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.kms">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>  //읽기권한
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/> //쓰기권한
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> //접근권한
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> //접근권한

    <application
        android:allowBackup="true"
        android:icon="@drawable/canvas"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KMS">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>